# Reference:
#   - https://github.com/actions/checkout
#   - https://github.com/actions/upload-artifact
#   - https://github.com/prefix-dev/setup-pixi
#   - https://github.com/pyvista/setup-headless-display-action

name: ci-tests-lock

on:
  schedule:
    # every day @ 00h03 except sunday (see ci-locks.yml)
    - cron: "3 0 * * 1-6"

  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash -l {0}

permissions:
  issues: write

jobs:
  test-lock:
    name: "test lock (${{ matrix.version }})"
    runs-on: "${{ matrix.os }}"

    env:
      ALLOW_PLOTTING: true
      PY_COLORS: "1"

    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-22.04"]
        version: ["py313"]

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: "environment configure"
        env:
          # Maximum cache period (in weeks) before forcing a cache refresh.
          CACHE_WEEKS: 2
        run: |
          echo "CACHE_PERIOD=$(date +%Y).$(expr $(date +%U) / ${CACHE_WEEKS})" >> ${GITHUB_ENV}
          echo "NAME=geovista-${{ matrix.version }}" >> ${GITHUB_ENV}

      - uses: pyvista/setup-headless-display-action@7d84ae825e6d9297a8e99bdbbae20d1b919a0b19

      - name: "setup pixi"
        uses: prefix-dev/setup-pixi@a0af7a228712d6121d37aba47adf55c1332c9c2e
        with:
          cache-write: false
          run-install: false

      - name: "refresh pixi manifest"
        run: |
          pixi update --json | pixi exec pixi-diff-to-markdown > diff.md

      - name: "upload artifacts"
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: ci-tests-lock-artifacts-${{ github.job }}-${{ strategy.job-index }}
          path: |
            ${{ github.workspace }}/diff.md
            ${{ github.workspace }}/pixi.lock

      - name: "cartopy cache"
        uses: ./.github/workflows/composite/cartopy-cache
        with:
          cache_period: ${{ env.CACHE_PERIOD }}

      - name: "geovista cache"
        uses: ./.github/workflows/composite/geovista-cache
        with:
          cache_period: ${{ env.CACHE_PERIOD }}
          pixi_environment: ${{ env.NAME }}

      - name: "test lock (${{ matrix.version }})"
        env:
          NAME: ${{ env.NAME }}
        run: |
          geovista --report
          xvfb-run -a pixi run --frozen --environment ${NAME} pytest --failed_image_dir test_images_failed

      - name: "report failure"
        if: ${{ failure() }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NAME: ${{ env.NAME }}
        run: |
          TITLE="chore: pixi.lock test failure"
          BODY=$(cat <<EOL
          The \`${{ matrix.version }}\` tests for the latest resolved \`pixi.lock\` have failed for the \`${NAME}\` environment.

          Please investigate GHA run-id [${{ github.run_id }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
          EOL
          )
          gh issue create --title "${TITLE}" --body "${BODY}" --label "bot" --label "new: issue" --repo "${{ github.repository }}"

      - name: "upload failed images"
        if: ${{ failure() }}
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: ci-tests-lock-failed-images-${{ github.job }}-${{ strategy.job-index }}
          path: ${{ github.workspace }}/test_images_failed
