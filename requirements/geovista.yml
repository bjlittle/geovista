name: geovista-dev

channels:
  - conda-forge

dependencies:

# setup dependencies
  - setuptools >=45
  - setuptools-scm >=7

# core dependencies
  - cartopy >=0.20
  - click
  - click-default-group
  - pooch
  - pykdtree
  - pyproj >=3.3
  - pyvista
  - pyvistaqt
  - vtk

# pantry dependencies
  - netcdf4

# cmap dependencies
  - cmocean
  - colorcet

# dev dependencies
  - pip
  - pre-commit

# test dependencies
  - codecov
  - pytest >=6.0
  - pytest-cov

# documentation dependencies
  - jupyter_sphinx
  - pydata-sphinx-theme
  - pythreejs
  - sphinx
  - sphinx-copybutton
