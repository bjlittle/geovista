name: geovista-dev-test

channels:
  - conda-forge

dependencies:

# setup dependencies
  - setuptools >=45
  - setuptools-scm >=7

# core dependencies
  - cartopy >=0.20
  - click
  - click-default-group
  - pooch
  - pyproj >=3.3
  - pyvista
  - pyvistaqt >0.37
  - vtk =9.2.2

# pantry dependencies
  - netcdf4

# cmap dependencies
  - cmocean
  - colorcet

# dev dependencies
  - pip
  - pre-commit

# test dependencies
  - codecov
  - pytest >=6.0
  - pytest-cov

# documentation dependencies
  - jupyter_sphinx
  - pydata-sphinx-theme
  - pythreejs
  - sphinx
  - sphinx-copybutton
